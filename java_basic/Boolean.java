package java_basic;

public class Boolean {
    public static void main(String[] args) {
        int andi = 80 , budi = 90;

        boolean IsPassed;
        if (andi >= 75 && budi >= 75) {
            IsPassed = true;
        } else {
            IsPassed = false;
        }
        System.out.println(IsPassed);

    }
}
